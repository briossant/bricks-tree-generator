import * as React from "react";

interface BranchSettings {
    length: number;
    curvature: number;
    direction: [number, number, number];
    curvatureAngle: number;

}

export const Branch: React.FC<BranchSettings> = ({length, curvature}) => {

    return <>

    </>
}